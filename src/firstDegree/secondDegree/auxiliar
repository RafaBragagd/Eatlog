return (
    foods.map((food) => (
      <div key={food.id}>
        <h3>{food.name}</h3>
        <p>{food.calories}</p>
        <p>{food.protein}</p>
        <p>{food.carbo}</p>
        <p>{food.fibers}</p>
        <p>{food.fats}</p>
    </div>
    ))
    
  )